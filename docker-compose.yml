version: '3.8'

services:
  facematch:
    build: .
    ports:
      - 3001:5000
    volumes:
      - ./config:/app/config
      - ./images:/app/images
      - ./results:/app/results
    environment:
      - FLASK_APP=app.py
      - FLASK_ENV=development
    command: python app.py