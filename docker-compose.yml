services:
  facematch:
    build: .
    container_name: facematch
    ports:
      - "8000:8000"
    env_file:
      - path: .env
        required: false
    volumes:
      # DeepFace downloads model weights on first use; persist them.
      - deepface-weights:/root/.deepface

volumes:
  deepface-weights:
